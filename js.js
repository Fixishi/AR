function changeModel(src) {     // Function to change the model in the main model viewer
    const mainModel = document.getElementById('main-model');    // Get the main model viewer
    mainModel.src = src;   // Change the model to the new model
}